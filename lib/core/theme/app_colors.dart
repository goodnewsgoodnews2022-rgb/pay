import 'package:flutter/material.dart';

class AppColors {
  // Global Structural Canvas Elements
  static const Color bgCanvas = Color(0xFF0A0E17);
  static const Color bgSurface = Color(0xFF161F30);
  
  // Clear Typography Hierarchy
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8F9BB3);

  // Team Assignment Signature Accent Colors
  static const Color dev1Silver = Color(0xFFE2E8F0); // Your signature accent
  static const Color dev2Green = Color(0xFF00E676);  // Onboarding & Fiat
  static const Color dev3Purple = Color(0xFF7C4DFF); // Dashboard & Web3

  // System Status Triggers
  static const Color error = Color(0xFFFF5252);
  static const Color success = Color(0xFF00E676);
}